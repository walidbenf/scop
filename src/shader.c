#include "shader.h"
#include <stdio.h>
#include <stdlib.h>

GLuint create_shader_program(const char *vertexPath, const char *fragmentPath) {
    // Logique de la compilation des shaders à implémenter ici
    return 0;
}