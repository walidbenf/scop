#include "renderer.hpp"
#include <GL/glew.h>

void render_object(const Object3D &obj, GLuint shaderProgram) {
    // Je dois implémenter la logique de rendu ici

	// éviter des problèmes de compilation je les caste en void
    (void)obj; 
    (void)shaderProgram;
}